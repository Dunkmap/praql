const QUESTIONS_DATA = {
  easy: [
    { id: 1, difficulty: "easy", question: "Retrieve all columns from the employees table.", dataset: "employees", expected_query: "SELECT * FROM employees", hint: "Use SELECT * to get all columns.", topic: "SELECT" },
    { id: 2, difficulty: "easy", question: "Get the names of all employees.", dataset: "employees", expected_query: "SELECT name FROM employees", hint: "Select just the 'name' column.", topic: "SELECT" },
    { id: 3, difficulty: "easy", question: "Get the unique department names from the employees table.", dataset: "employees", expected_query: "SELECT DISTINCT department FROM employees", hint: "Use DISTINCT to remove duplicates.", topic: "DISTINCT" },
    { id: 4, difficulty: "easy", question: "Retrieve all employees with a salary greater than 5000.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE salary > 5000", hint: "Use WHERE to filter by salary.", topic: "WHERE" },
    { id: 5, difficulty: "easy", question: "Get employees who work in the IT department.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE department = 'IT'", hint: "Filter by department = 'IT'.", topic: "WHERE" },
    { id: 6, difficulty: "easy", question: "Find employees in IT department with salary above 6000.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE department = 'IT' AND salary > 6000", hint: "Combine conditions with AND.", topic: "AND" },
    { id: 7, difficulty: "easy", question: "Find employees who work in HR or Marketing.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE department = 'HR' OR department = 'Marketing'", hint: "Use OR to match multiple departments.", topic: "OR" },
    { id: 8, difficulty: "easy", question: "Get all employees whose name starts with 'J'.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE name LIKE 'J%'", hint: "Use LIKE with 'J%' pattern.", topic: "LIKE" },
    { id: 9, difficulty: "easy", question: "Find employees with salary between 4500 and 6000.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE salary BETWEEN 4500 AND 6000", hint: "Use BETWEEN for range filtering.", topic: "BETWEEN" },
    { id: 10, difficulty: "easy", question: "Get employees in the IT, HR, or Finance departments using IN.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE department IN ('IT', 'HR', 'Finance')", hint: "Use IN with a list of values.", topic: "IN" },
    { id: 11, difficulty: "easy", question: "Find employees who do not have a manager (manager_id is NULL).", dataset: "employees", expected_query: "SELECT * FROM employees WHERE manager_id IS NULL", hint: "Use IS NULL for NULL comparisons.", topic: "IS NULL" },
    { id: 12, difficulty: "easy", question: "List all employees ordered by salary from highest to lowest.", dataset: "employees", expected_query: "SELECT * FROM employees ORDER BY salary DESC", hint: "Use ORDER BY with DESC.", topic: "ORDER BY" },
    { id: 13, difficulty: "easy", question: "Get the top 5 highest-paid employees.", dataset: "employees", expected_query: "SELECT * FROM employees ORDER BY salary DESC LIMIT 5", hint: "Combine ORDER BY DESC with LIMIT.", topic: "LIMIT" },
    { id: 14, difficulty: "easy", question: "Count the total number of employees.", dataset: "employees", expected_query: "SELECT COUNT(*) AS total FROM employees", hint: "Use COUNT(*) to count rows.", topic: "COUNT" },
    { id: 15, difficulty: "easy", question: "Find the maximum salary in the employees table.", dataset: "employees", expected_query: "SELECT MAX(salary) AS max_salary FROM employees", hint: "Use MAX() function.", topic: "MAX" },
    { id: 16, difficulty: "easy", question: "Calculate the average salary of all employees.", dataset: "employees", expected_query: "SELECT AVG(salary) AS avg_salary FROM employees", hint: "Use AVG() function.", topic: "AVG" },
    { id: 17, difficulty: "easy", question: "Get all products from the products table.", dataset: "products", expected_query: "SELECT * FROM products", hint: "Use SELECT * FROM products.", topic: "SELECT" },
    { id: 18, difficulty: "easy", question: "Find products with price less than 50.", dataset: "products", expected_query: "SELECT * FROM products WHERE price < 50", hint: "Use WHERE with < operator.", topic: "WHERE" },
    { id: 19, difficulty: "easy", question: "Get all Electronics products.", dataset: "products", expected_query: "SELECT * FROM products WHERE category = 'Electronics'", hint: "Filter by category.", topic: "WHERE" },
    { id: 20, difficulty: "easy", question: "List products ordered by price (ascending).", dataset: "products", expected_query: "SELECT * FROM products ORDER BY price ASC", hint: "Use ORDER BY with ASC.", topic: "ORDER BY" },
    { id: 21, difficulty: "easy", question: "Count the number of products in each category.", dataset: "products", expected_query: "SELECT category, COUNT(*) AS count FROM products GROUP BY category", hint: "Use GROUP BY with COUNT.", topic: "GROUP BY" },
    { id: 22, difficulty: "easy", question: "Get the total revenue from the sales table.", dataset: "sales", expected_query: "SELECT SUM(revenue) AS total_revenue FROM sales", hint: "Use SUM() on the revenue column.", topic: "SUM" },
    { id: 23, difficulty: "easy", question: "List all delivered orders.", dataset: "orders", expected_query: "SELECT * FROM orders WHERE status = 'Delivered'", hint: "Filter by status = 'Delivered'.", topic: "WHERE" },
    { id: 24, difficulty: "easy", question: "Find customers from the USA.", dataset: "customers", expected_query: "SELECT * FROM customers WHERE country = 'USA'", hint: "Filter by country.", topic: "WHERE" },
    { id: 25, difficulty: "easy", question: "Get the names and cities of all customers.", dataset: "customers", expected_query: "SELECT name, city FROM customers", hint: "Select just name and city columns.", topic: "SELECT" },
    { id: 26, difficulty: "easy", question: "Find the minimum product price.", dataset: "products", expected_query: "SELECT MIN(price) AS min_price FROM products", hint: "Use MIN() function.", topic: "MIN" },
    { id: 27, difficulty: "easy", question: "Get employees who live in New York.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE city = 'New York'", hint: "Filter by city.", topic: "WHERE" },
    { id: 28, difficulty: "easy", question: "Count the number of orders per status.", dataset: "orders", expected_query: "SELECT status, COUNT(*) AS count FROM orders GROUP BY status", hint: "Use GROUP BY with COUNT.", topic: "GROUP BY" },
    { id: 29, difficulty: "easy", question: "Get all employees sorted alphabetically by name.", dataset: "employees", expected_query: "SELECT * FROM employees ORDER BY name ASC", hint: "Use ORDER BY name.", topic: "ORDER BY" },
    { id: 30, difficulty: "easy", question: "Find products with a rating of 4.5 or higher.", dataset: "products", expected_query: "SELECT * FROM products WHERE rating >= 4.5", hint: "Use WHERE with >= operator.", topic: "WHERE" },
    { id: 31, difficulty: "easy", question: "Get the total stock for all products.", dataset: "products", expected_query: "SELECT SUM(stock) AS total_stock FROM products", hint: "Use SUM() on stock column.", topic: "SUM" },
    { id: 32, difficulty: "easy", question: "Find employees whose email contains 'company'.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE email LIKE '%company%'", hint: "Use LIKE with %company% pattern.", topic: "LIKE" },
    { id: 33, difficulty: "easy", question: "Get the 3 cheapest products.", dataset: "products", expected_query: "SELECT * FROM products ORDER BY price ASC LIMIT 3", hint: "ORDER BY price and LIMIT 3.", topic: "LIMIT" },
    { id: 34, difficulty: "easy", question: "Count unique cities where employees work.", dataset: "employees", expected_query: "SELECT COUNT(DISTINCT city) AS unique_cities FROM employees", hint: "Use COUNT(DISTINCT city).", topic: "DISTINCT" },
    { id: 35, difficulty: "easy", question: "Find all sales in the East region.", dataset: "sales", expected_query: "SELECT * FROM sales WHERE region = 'East'", hint: "Filter by region.", topic: "WHERE" },
    { id: 36, difficulty: "easy", question: "Get the average product rating.", dataset: "products", expected_query: "SELECT AVG(rating) AS avg_rating FROM products", hint: "Use AVG() on rating column.", topic: "AVG" },
    { id: 37, difficulty: "easy", question: "Find orders with total amount over 500.", dataset: "orders", expected_query: "SELECT * FROM orders WHERE total_amount > 500", hint: "Filter by total_amount.", topic: "WHERE" },
    { id: 38, difficulty: "easy", question: "Get unique countries from the customers table.", dataset: "customers", expected_query: "SELECT DISTINCT country FROM customers", hint: "Use DISTINCT on country.", topic: "DISTINCT" },
    { id: 39, difficulty: "easy", question: "Find the total salary expense per department.", dataset: "employees", expected_query: "SELECT department, SUM(salary) AS total_salary FROM employees GROUP BY department", hint: "GROUP BY department and SUM salary.", topic: "GROUP BY" },
    { id: 40, difficulty: "easy", question: "Get employees who are 30 years old or younger.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE age <= 30", hint: "Use WHERE age <= 30.", topic: "WHERE" },
    { id: 41, difficulty: "easy", question: "Find products supplied by TechCorp.", dataset: "products", expected_query: "SELECT * FROM products WHERE supplier = 'TechCorp'", hint: "Filter by supplier.", topic: "WHERE" },
    { id: 42, difficulty: "easy", question: "Get the number of employees per city.", dataset: "employees", expected_query: "SELECT city, COUNT(*) AS count FROM employees GROUP BY city", hint: "GROUP BY city with COUNT.", topic: "GROUP BY" },
    { id: 43, difficulty: "easy", question: "Find platinum tier customers.", dataset: "customers", expected_query: "SELECT * FROM customers WHERE loyalty_tier = 'Platinum'", hint: "Filter by loyalty_tier.", topic: "WHERE" },
    { id: 44, difficulty: "easy", question: "Get total sales revenue per quarter.", dataset: "sales", expected_query: "SELECT quarter, SUM(revenue) AS total FROM sales GROUP BY quarter", hint: "GROUP BY quarter.", topic: "GROUP BY" },
    { id: 45, difficulty: "easy", question: "Find employees not in the IT department.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE department != 'IT'", hint: "Use != or <> operator.", topic: "WHERE" },
    { id: 46, difficulty: "easy", question: "Get average salary per department.", dataset: "employees", expected_query: "SELECT department, AVG(salary) AS avg_salary FROM employees GROUP BY department", hint: "GROUP BY with AVG.", topic: "GROUP BY" },
    { id: 47, difficulty: "easy", question: "Find the most expensive product.", dataset: "products", expected_query: "SELECT * FROM products ORDER BY price DESC LIMIT 1", hint: "ORDER BY price DESC and LIMIT 1.", topic: "LIMIT" },
    { id: 48, difficulty: "easy", question: "Get all pending orders.", dataset: "orders", expected_query: "SELECT * FROM orders WHERE status = 'Pending'", hint: "Filter by status = 'Pending'.", topic: "WHERE" },
    { id: 49, difficulty: "easy", question: "Find total quantity sold per product.", dataset: "sales", expected_query: "SELECT product_id, SUM(quantity) AS total_qty FROM sales GROUP BY product_id", hint: "GROUP BY product_id with SUM.", topic: "GROUP BY" },
    { id: 50, difficulty: "easy", question: "Get employees hired after 2020-01-01.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE hire_date > '2020-01-01'", hint: "Use WHERE with date comparison.", topic: "WHERE" }
  ],
  medium: [
    { id: 51, difficulty: "medium", question: "Find departments with more than 3 employees.", dataset: "employees", expected_query: "SELECT department, COUNT(*) AS emp_count FROM employees GROUP BY department HAVING COUNT(*) > 3", hint: "Use HAVING after GROUP BY.", topic: "HAVING" },
    { id: 52, difficulty: "medium", question: "Get each employee's name and their manager's name.", dataset: "employees", expected_query: "SELECT e.name AS employee, m.name AS manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.id", hint: "Use SELF JOIN with LEFT JOIN.", topic: "SELF JOIN" },
    { id: 53, difficulty: "medium", question: "List all orders with customer names.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT o.id, c.name AS customer, o.total_amount, o.status FROM orders o INNER JOIN customers c ON o.customer_id = c.id", hint: "JOIN orders with customers.", topic: "INNER JOIN" },
    { id: 54, difficulty: "medium", question: "Find customers who have placed at least one order.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT DISTINCT c.name FROM customers c INNER JOIN orders o ON c.id = o.customer_id", hint: "JOIN customers and orders.", topic: "INNER JOIN" },
    { id: 55, difficulty: "medium", question: "Get all customers, including those without orders.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT c.name, o.id AS order_id FROM customers c LEFT JOIN orders o ON c.id = o.customer_id", hint: "Use LEFT JOIN to include all customers.", topic: "LEFT JOIN" },
    { id: 56, difficulty: "medium", question: "Find employees who earn more than the average salary.", dataset: "employees", expected_query: "SELECT name, salary FROM employees WHERE salary > (SELECT AVG(salary) FROM employees)", hint: "Use a subquery with AVG.", topic: "Subquery" },
    { id: 57, difficulty: "medium", question: "Get the names of orders along with product names.", dataset: "orders", datasets: ["orders", "products"], expected_query: "SELECT o.id, p.name AS product, o.quantity, o.total_amount FROM orders o INNER JOIN products p ON o.product_id = p.id", hint: "JOIN orders and products.", topic: "INNER JOIN" },
    { id: 58, difficulty: "medium", question: "Categorize employees by salary: High (>=7000), Medium (>=5000), Low (<5000).", dataset: "employees", expected_query: "SELECT name, salary, CASE WHEN salary >= 7000 THEN 'High' WHEN salary >= 5000 THEN 'Medium' ELSE 'Low' END AS tier FROM employees", hint: "Use a CASE expression.", topic: "CASE" },
    { id: 59, difficulty: "medium", question: "Find products with above-average price.", dataset: "products", expected_query: "SELECT * FROM products WHERE price > (SELECT AVG(price) FROM products)", hint: "Use a subquery for average price.", topic: "Subquery" },
    { id: 60, difficulty: "medium", question: "Get the total order amount for each customer.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT c.name, SUM(o.total_amount) AS total_spent FROM customers c INNER JOIN orders o ON c.id = o.customer_id GROUP BY c.name", hint: "JOIN and GROUP BY customer.", topic: "GROUP BY" },
    { id: 61, difficulty: "medium", question: "Find the department with the highest average salary.", dataset: "employees", expected_query: "SELECT department, AVG(salary) AS avg_salary FROM employees GROUP BY department ORDER BY avg_salary DESC LIMIT 1", hint: "GROUP BY, ORDER BY and LIMIT 1.", topic: "GROUP BY" },
    { id: 62, difficulty: "medium", question: "List employees and their rank by salary (use RANK).", dataset: "employees", expected_query: "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS salary_rank FROM employees", hint: "Use RANK() window function.", topic: "RANK" },
    { id: 63, difficulty: "medium", question: "Get the running total of sales revenue ordered by date.", dataset: "sales", expected_query: "SELECT sale_date, revenue, SUM(revenue) OVER (ORDER BY sale_date) AS running_total FROM sales", hint: "Use SUM() with OVER clause.", topic: "Window Aggregates" },
    { id: 64, difficulty: "medium", question: "Find products with stock below 50.", dataset: "products", expected_query: "SELECT name, stock FROM products WHERE stock < 50", hint: "Simple WHERE filter.", topic: "WHERE" },
    { id: 65, difficulty: "medium", question: "Get the count of orders per month.", dataset: "orders", expected_query: "SELECT STRFTIME('%Y-%m', order_date) AS month, COUNT(*) AS order_count FROM orders GROUP BY STRFTIME('%Y-%m', order_date)", hint: "Use STRFTIME to extract month.", topic: "Date Functions" },
    { id: 66, difficulty: "medium", question: "Find sales employees (those in the sales table) and their total revenue.", dataset: "sales", datasets: ["sales", "employees"], expected_query: "SELECT e.name, SUM(s.revenue) AS total_revenue FROM employees e INNER JOIN sales s ON e.id = s.employee_id GROUP BY e.name", hint: "JOIN employees and sales.", topic: "INNER JOIN" },
    { id: 67, difficulty: "medium", question: "Create a view of high-salary employees (salary > 6000) and select from it.", dataset: "employees", expected_query: "CREATE VIEW IF NOT EXISTS high_salary AS SELECT name, department, salary FROM employees WHERE salary > 6000; SELECT * FROM high_salary", hint: "Use CREATE VIEW then SELECT.", topic: "CREATE VIEW" },
    { id: 68, difficulty: "medium", question: "Get employees with their department rank by salary.", dataset: "employees", expected_query: "SELECT name, department, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS dept_rank FROM employees", hint: "Use ROW_NUMBER with PARTITION BY.", topic: "ROW_NUMBER" },
    { id: 69, difficulty: "medium", question: "Find the second highest salary.", dataset: "employees", expected_query: "SELECT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 1", hint: "Use LIMIT 1 OFFSET 1 after ORDER BY.", topic: "LIMIT" },
    { id: 70, difficulty: "medium", question: "Get each product's price compared to the category average.", dataset: "products", expected_query: "SELECT name, category, price, AVG(price) OVER (PARTITION BY category) AS cat_avg FROM products", hint: "Use AVG() with PARTITION BY.", topic: "Window Aggregates" },
    { id: 71, difficulty: "medium", question: "Find customers who have never placed an order.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT c.name FROM customers c LEFT JOIN orders o ON c.id = o.customer_id WHERE o.id IS NULL", hint: "LEFT JOIN with IS NULL check.", topic: "LEFT JOIN" },
    { id: 72, difficulty: "medium", question: "Get sales data with employee and product names.", dataset: "sales", datasets: ["sales", "employees", "products"], expected_query: "SELECT e.name AS employee, p.name AS product, s.revenue FROM sales s INNER JOIN employees e ON s.employee_id = e.id INNER JOIN products p ON s.product_id = p.id", hint: "Multiple JOINs.", topic: "INNER JOIN" },
    { id: 73, difficulty: "medium", question: "Assign salary quartiles (1-4) to employees.", dataset: "employees", expected_query: "SELECT name, salary, NTILE(4) OVER (ORDER BY salary) AS quartile FROM employees", hint: "Use NTILE(4) window function.", topic: "NTILE" },
    { id: 74, difficulty: "medium", question: "Get employees and the difference from previous salary (ordered by salary).", dataset: "employees", expected_query: "SELECT name, salary, salary - LAG(salary) OVER (ORDER BY salary) AS salary_diff FROM employees ORDER BY salary", hint: "Use LAG() window function.", topic: "LAG" },
    { id: 75, difficulty: "medium", question: "Find the total revenue per region.", dataset: "sales", expected_query: "SELECT region, SUM(revenue) AS total FROM sales GROUP BY region ORDER BY total DESC", hint: "GROUP BY region.", topic: "GROUP BY" },
    { id: 76, difficulty: "medium", question: "Get all categories with more than 4 products.", dataset: "products", expected_query: "SELECT category, COUNT(*) AS count FROM products GROUP BY category HAVING COUNT(*) > 4", hint: "Use HAVING to filter groups.", topic: "HAVING" },
    { id: 77, difficulty: "medium", question: "Find the employee with the highest salary in each department.", dataset: "employees", expected_query: "SELECT department, name, salary FROM employees e WHERE salary = (SELECT MAX(salary) FROM employees e2 WHERE e2.department = e.department)", hint: "Use a correlated subquery.", topic: "Correlated Subquery" },
    { id: 78, difficulty: "medium", question: "Get employee names in uppercase.", dataset: "employees", expected_query: "SELECT UPPER(name) AS name_upper, department FROM employees", hint: "Use UPPER() function.", topic: "String Functions" },
    { id: 79, difficulty: "medium", question: "Find the average order value per customer who has more than 2 orders.", dataset: "orders", expected_query: "SELECT customer_id, AVG(total_amount) AS avg_order, COUNT(*) AS order_count FROM orders GROUP BY customer_id HAVING COUNT(*) > 2", hint: "GROUP BY, HAVING, and AVG.", topic: "HAVING" },
    { id: 80, difficulty: "medium", question: "Get products and their rank within each category by price.", dataset: "products", expected_query: "SELECT name, category, price, DENSE_RANK() OVER (PARTITION BY category ORDER BY price DESC) AS price_rank FROM products", hint: "Use DENSE_RANK with PARTITION BY.", topic: "DENSE_RANK" },
    { id: 81, difficulty: "medium", question: "Find the hire year with the most employees.", dataset: "employees", expected_query: "SELECT STRFTIME('%Y', hire_date) AS year, COUNT(*) AS count FROM employees GROUP BY STRFTIME('%Y', hire_date) ORDER BY count DESC LIMIT 1", hint: "Use STRFTIME to extract year.", topic: "Date Functions" },
    { id: 82, difficulty: "medium", question: "List departments and whether their avg salary is above or below company average.", dataset: "employees", expected_query: "SELECT department, AVG(salary) AS dept_avg, CASE WHEN AVG(salary) > (SELECT AVG(salary) FROM employees) THEN 'Above' ELSE 'Below' END AS vs_company FROM employees GROUP BY department", hint: "Use CASE with subquery.", topic: "CASE" },
    { id: 83, difficulty: "medium", question: "Get the order count and total amount per order status.", dataset: "orders", expected_query: "SELECT status, COUNT(*) AS count, SUM(total_amount) AS total FROM orders GROUP BY status", hint: "GROUP BY status.", topic: "GROUP BY" },
    { id: 84, difficulty: "medium", question: "Find employees who share a city with at least one other employee.", dataset: "employees", expected_query: "SELECT * FROM employees WHERE city IN (SELECT city FROM employees GROUP BY city HAVING COUNT(*) > 1)", hint: "Use IN with a grouped subquery.", topic: "IN Subquery" },
    { id: 85, difficulty: "medium", question: "Get each employee's salary and the department maximum salary.", dataset: "employees", expected_query: "SELECT name, department, salary, MAX(salary) OVER (PARTITION BY department) AS dept_max FROM employees", hint: "Use MAX() with PARTITION BY.", topic: "Window Aggregates" },
    { id: 86, difficulty: "medium", question: "Find the top-selling product by quantity.", dataset: "sales", datasets: ["sales", "products"], expected_query: "SELECT p.name, SUM(s.quantity) AS total_qty FROM sales s INNER JOIN products p ON s.product_id = p.id GROUP BY p.name ORDER BY total_qty DESC LIMIT 1", hint: "JOIN, GROUP BY, ORDER BY, LIMIT.", topic: "GROUP BY" },
    { id: 87, difficulty: "medium", question: "Get customers sorted by total spend (descending).", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT c.name, COALESCE(SUM(o.total_amount), 0) AS total_spent FROM customers c LEFT JOIN orders o ON c.id = o.customer_id GROUP BY c.name ORDER BY total_spent DESC", hint: "LEFT JOIN with SUM and ORDER BY.", topic: "LEFT JOIN" },
    { id: 88, difficulty: "medium", question: "Find products that have been ordered (exist in orders).", dataset: "orders", datasets: ["orders", "products"], expected_query: "SELECT DISTINCT p.name FROM products p WHERE EXISTS (SELECT 1 FROM orders o WHERE o.product_id = p.id)", hint: "Use EXISTS with a subquery.", topic: "EXISTS" },
    { id: 89, difficulty: "medium", question: "Get the number of days each employee has been employed (from 2026-01-01).", dataset: "employees", expected_query: "SELECT name, hire_date, CAST(JULIANDAY('2026-01-01') - JULIANDAY(hire_date) AS INTEGER) AS days_employed FROM employees ORDER BY days_employed DESC", hint: "Use JULIANDAY for date math.", topic: "Date Functions" },
    { id: 90, difficulty: "medium", question: "Find the average product price per supplier.", dataset: "products", expected_query: "SELECT supplier, AVG(price) AS avg_price FROM products GROUP BY supplier ORDER BY avg_price DESC", hint: "GROUP BY supplier.", topic: "GROUP BY" },
    { id: 91, difficulty: "medium", question: "Get each sale and the next sale's revenue (by date).", dataset: "sales", expected_query: "SELECT sale_date, revenue, LEAD(revenue) OVER (ORDER BY sale_date) AS next_sale_revenue FROM sales", hint: "Use LEAD() window function.", topic: "LEAD" },
    { id: 92, difficulty: "medium", question: "Find employees whose salary is above average for their city.", dataset: "employees", expected_query: "SELECT name, city, salary FROM employees e WHERE salary > (SELECT AVG(salary) FROM employees e2 WHERE e2.city = e.city)", hint: "Use a correlated subquery.", topic: "Correlated Subquery" },
    { id: 93, difficulty: "medium", question: "Combine a list of employee names from IT and customer names from USA.", dataset: "employees", datasets: ["employees", "customers"], expected_query: "SELECT name FROM employees WHERE department = 'IT' UNION SELECT name FROM customers WHERE country = 'USA'", hint: "Use UNION to combine two SELECTs.", topic: "UNION" },
    { id: 94, difficulty: "medium", question: "Find products with the same category as 'Laptop Pro'.", dataset: "products", expected_query: "SELECT * FROM products WHERE category = (SELECT category FROM products WHERE name = 'Laptop Pro') AND name != 'Laptop Pro'", hint: "Use a subquery to find the category.", topic: "Subquery" },
    { id: 95, difficulty: "medium", question: "Get total revenue per employee and per region.", dataset: "sales", expected_query: "SELECT employee_id, region, SUM(revenue) AS total FROM sales GROUP BY employee_id, region", hint: "GROUP BY two columns.", topic: "GROUP BY" }
  ],
  hard: [
    { id: 96, difficulty: "hard", question: "Find the employee with the 3rd highest salary.", dataset: "employees", expected_query: "SELECT name, salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 2", hint: "Use LIMIT 1 OFFSET 2.", topic: "LIMIT" },
    { id: 97, difficulty: "hard", question: "For each department, find the employee with the highest salary along with their name.", dataset: "employees", expected_query: "SELECT name, department, salary FROM employees e WHERE salary = (SELECT MAX(salary) FROM employees e2 WHERE e2.department = e.department)", hint: "Use a correlated subquery with MAX.", topic: "Correlated Subquery" },
    { id: 98, difficulty: "hard", question: "Calculate each employee's salary as a percentage of total company salary.", dataset: "employees", expected_query: "SELECT name, salary, ROUND(salary * 100.0 / SUM(salary) OVER (), 2) AS pct_of_total FROM employees ORDER BY pct_of_total DESC", hint: "Use SUM() OVER() for total.", topic: "Window Aggregates" },
    { id: 99, difficulty: "hard", question: "Get a running total of order amounts sorted by date, along with customer names.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT o.order_date, c.name, o.total_amount, SUM(o.total_amount) OVER (ORDER BY o.order_date) AS running_total FROM orders o INNER JOIN customers c ON o.customer_id = c.id", hint: "JOIN with window function.", topic: "Window Aggregates" },
    { id: 100, difficulty: "hard", question: "Find departments where every employee earns more than 4500.", dataset: "employees", expected_query: "SELECT department FROM employees GROUP BY department HAVING MIN(salary) > 4500", hint: "Use HAVING with MIN.", topic: "HAVING" },
    { id: 101, difficulty: "hard", question: "Get the top 2 highest-paid employees per department.", dataset: "employees", expected_query: "SELECT * FROM (SELECT name, department, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS rn FROM employees) WHERE rn <= 2", hint: "ROW_NUMBER in a subquery.", topic: "ROW_NUMBER" },
    { id: 102, difficulty: "hard", question: "Find customers whose total spend exceeds the average customer spend.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT c.name, SUM(o.total_amount) AS total FROM customers c INNER JOIN orders o ON c.id = o.customer_id GROUP BY c.name HAVING SUM(o.total_amount) > (SELECT AVG(t) FROM (SELECT SUM(total_amount) AS t FROM orders GROUP BY customer_id))", hint: "Nested subquery with HAVING.", topic: "Subquery" },
    { id: 103, difficulty: "hard", question: "Calculate month-over-month revenue change for sales.", dataset: "sales", expected_query: "SELECT month, total, total - LAG(total) OVER (ORDER BY month) AS change FROM (SELECT STRFTIME('%Y-%m', sale_date) AS month, SUM(revenue) AS total FROM sales GROUP BY STRFTIME('%Y-%m', sale_date))", hint: "Combine GROUP BY with LAG.", topic: "LAG" },
    { id: 104, difficulty: "hard", question: "Find products that have never been sold.", dataset: "sales", datasets: ["sales", "products"], expected_query: "SELECT p.name FROM products p WHERE NOT EXISTS (SELECT 1 FROM sales s WHERE s.product_id = p.id)", hint: "Use NOT EXISTS.", topic: "EXISTS" },
    { id: 105, difficulty: "hard", question: "Get the cumulative percentage of total revenue per region.", dataset: "sales", expected_query: "SELECT region, SUM(revenue) AS region_total, ROUND(SUM(revenue) * 100.0 / (SELECT SUM(revenue) FROM sales), 2) AS pct FROM sales GROUP BY region ORDER BY pct DESC", hint: "Use a subquery for total.", topic: "Subquery" },
    { id: 106, difficulty: "hard", question: "Find employees who manage other employees.", dataset: "employees", expected_query: "SELECT DISTINCT m.name AS manager FROM employees e INNER JOIN employees m ON e.manager_id = m.id", hint: "Self join to find managers.", topic: "SELF JOIN" },
    { id: 107, difficulty: "hard", question: "Get each order with the product name, customer name, and order rank by total_amount per customer.", dataset: "orders", datasets: ["orders", "customers", "products"], expected_query: "SELECT c.name AS customer, p.name AS product, o.total_amount, RANK() OVER (PARTITION BY c.name ORDER BY o.total_amount DESC) AS order_rank FROM orders o JOIN customers c ON o.customer_id = c.id JOIN products p ON o.product_id = p.id", hint: "Multiple JOINs with RANK.", topic: "RANK" },
    { id: 108, difficulty: "hard", question: "Find the quarter with the highest average revenue per sale.", dataset: "sales", expected_query: "SELECT quarter, AVG(revenue) AS avg_rev FROM sales GROUP BY quarter ORDER BY avg_rev DESC LIMIT 1", hint: "GROUP BY quarter, ORDER BY, LIMIT.", topic: "GROUP BY" },
    { id: 109, difficulty: "hard", question: "For each employee, show their salary compared to the next higher salary.", dataset: "employees", expected_query: "SELECT name, salary, LEAD(salary) OVER (ORDER BY salary) AS next_salary, LEAD(salary) OVER (ORDER BY salary) - salary AS gap FROM employees ORDER BY salary", hint: "Use LEAD() to get next value.", topic: "LEAD" },
    { id: 110, difficulty: "hard", question: "Find all pairs of employees in the same department.", dataset: "employees", expected_query: "SELECT e1.name AS employee1, e2.name AS employee2, e1.department FROM employees e1 INNER JOIN employees e2 ON e1.department = e2.department AND e1.id < e2.id ORDER BY e1.department", hint: "Self join with id < id.", topic: "SELF JOIN" },
    { id: 111, difficulty: "hard", question: "Get the dense rank of products by price within each supplier.", dataset: "products", expected_query: "SELECT name, supplier, price, DENSE_RANK() OVER (PARTITION BY supplier ORDER BY price DESC) AS price_rank FROM products", hint: "DENSE_RANK with PARTITION BY.", topic: "DENSE_RANK" },
    { id: 112, difficulty: "hard", question: "Find the customer who placed the most orders.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT c.name, COUNT(*) AS order_count FROM orders o INNER JOIN customers c ON o.customer_id = c.id GROUP BY c.name ORDER BY order_count DESC LIMIT 1", hint: "JOIN, GROUP BY, ORDER BY, LIMIT.", topic: "GROUP BY" },
    { id: 113, difficulty: "hard", question: "Calculate the daily revenue and its 3-day moving average.", dataset: "sales", expected_query: "SELECT sale_date, revenue, AVG(revenue) OVER (ORDER BY sale_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) AS moving_avg FROM sales", hint: "Window frame ROWS BETWEEN.", topic: "Window Aggregates" },
    { id: 114, difficulty: "hard", question: "Get employees, their manager, and their manager's manager (grandmanager).", dataset: "employees", expected_query: "SELECT e.name AS employee, m.name AS manager, gm.name AS grand_manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.id LEFT JOIN employees gm ON m.manager_id = gm.id", hint: "Triple self join.", topic: "SELF JOIN" },
    { id: 115, difficulty: "hard", question: "Find departments where the average salary is above the company median salary.", dataset: "employees", expected_query: "SELECT department, AVG(salary) AS dept_avg FROM employees GROUP BY department HAVING AVG(salary) > (SELECT salary FROM employees ORDER BY salary LIMIT 1 OFFSET (SELECT COUNT(*)/2 FROM employees))", hint: "Subquery for median.", topic: "Subquery" },
    { id: 116, difficulty: "hard", question: "Calculate each employee's salary percentile rank.", dataset: "employees", expected_query: "SELECT name, salary, ROUND(PERCENT_RANK() OVER (ORDER BY salary) * 100, 1) AS percentile FROM employees", hint: "Use PERCENT_RANK().", topic: "Window Functions" },
    { id: 117, difficulty: "hard", question: "Get the most purchased product per customer.", dataset: "orders", expected_query: "SELECT customer_id, product_id, quantity FROM (SELECT customer_id, product_id, quantity, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY quantity DESC) AS rn FROM orders) WHERE rn = 1", hint: "ROW_NUMBER in subquery.", topic: "ROW_NUMBER" },
    { id: 118, difficulty: "hard", question: "Find regions where total revenue exceeds the average regional revenue.", dataset: "sales", expected_query: "SELECT region, SUM(revenue) AS total FROM sales GROUP BY region HAVING SUM(revenue) > (SELECT AVG(t) FROM (SELECT SUM(revenue) AS t FROM sales GROUP BY region))", hint: "Nested subquery for average.", topic: "HAVING" },
    { id: 119, difficulty: "hard", question: "Get a pivot-like view showing employee count per department per city.", dataset: "employees", expected_query: "SELECT city, SUM(CASE WHEN department = 'IT' THEN 1 ELSE 0 END) AS IT, SUM(CASE WHEN department = 'HR' THEN 1 ELSE 0 END) AS HR, SUM(CASE WHEN department = 'Finance' THEN 1 ELSE 0 END) AS Finance, SUM(CASE WHEN department = 'Marketing' THEN 1 ELSE 0 END) AS Marketing FROM employees GROUP BY city", hint: "CASE inside SUM for pivoting.", topic: "CASE" },
    { id: 120, difficulty: "hard", question: "Find the employee with the longest name in each department.", dataset: "employees", expected_query: "SELECT name, department, LENGTH(name) AS name_len FROM employees e WHERE LENGTH(name) = (SELECT MAX(LENGTH(name)) FROM employees e2 WHERE e2.department = e.department)", hint: "Correlated subquery with LENGTH.", topic: "String Functions" },
    { id: 121, difficulty: "hard", question: "Calculate year-over-year hire count comparison.", dataset: "employees", expected_query: "SELECT year, count, LAG(count) OVER (ORDER BY year) AS prev_year, count - LAG(count) OVER (ORDER BY year) AS change FROM (SELECT STRFTIME('%Y', hire_date) AS year, COUNT(*) AS count FROM employees GROUP BY STRFTIME('%Y', hire_date))", hint: "Derived table with LAG.", topic: "LAG" },
    { id: 122, difficulty: "hard", question: "Get ordered list of products with cumulative stock percentage.", dataset: "products", expected_query: "SELECT name, stock, SUM(stock) OVER (ORDER BY stock DESC) AS cumulative, ROUND(SUM(stock) OVER (ORDER BY stock DESC) * 100.0 / SUM(stock) OVER (), 1) AS cum_pct FROM products ORDER BY stock DESC", hint: "Cumulative sum as percentage.", topic: "Window Aggregates" },
    { id: 123, difficulty: "hard", question: "Find the top spending customer per country.", dataset: "orders", datasets: ["orders", "customers"], expected_query: "SELECT * FROM (SELECT c.country, c.name, SUM(o.total_amount) AS total, ROW_NUMBER() OVER (PARTITION BY c.country ORDER BY SUM(o.total_amount) DESC) AS rn FROM customers c JOIN orders o ON c.id = o.customer_id GROUP BY c.country, c.name) WHERE rn = 1", hint: "GROUP BY, ROW_NUMBER, subquery.", topic: "ROW_NUMBER" },
    { id: 124, difficulty: "hard", question: "Create an employees summary showing count, avg salary, min and max salary per department, only for departments with avg salary > 5000.", dataset: "employees", expected_query: "SELECT department, COUNT(*) AS count, ROUND(AVG(salary),0) AS avg_sal, MIN(salary) AS min_sal, MAX(salary) AS max_sal FROM employees GROUP BY department HAVING AVG(salary) > 5000", hint: "Multiple aggregates with HAVING.", topic: "HAVING" },
    { id: 125, difficulty: "hard", question: "Find products where the price is above average for their category and the stock is below average for their category.", dataset: "products", expected_query: "SELECT name, category, price, stock FROM products p WHERE price > (SELECT AVG(price) FROM products p2 WHERE p2.category = p.category) AND stock < (SELECT AVG(stock) FROM products p3 WHERE p3.category = p.category)", hint: "Two correlated subqueries.", topic: "Correlated Subquery" },
    { id: 126, difficulty: "hard", question: "Calculate each sale's revenue as a percentage of that region's total.", dataset: "sales", expected_query: "SELECT sale_date, region, revenue, ROUND(revenue * 100.0 / SUM(revenue) OVER (PARTITION BY region), 2) AS pct_of_region FROM sales ORDER BY region, pct_of_region DESC", hint: "SUM OVER PARTITION BY.", topic: "Window Aggregates" },
    { id: 127, difficulty: "hard", question: "Find orders where the total_amount exceeds twice the average order amount.", dataset: "orders", expected_query: "SELECT * FROM orders WHERE total_amount > 2 * (SELECT AVG(total_amount) FROM orders)", hint: "Subquery with multiplication.", topic: "Subquery" },
    { id: 128, difficulty: "hard", question: "Get the gap between each employee's salary and the department's highest salary.", dataset: "employees", expected_query: "SELECT name, department, salary, MAX(salary) OVER (PARTITION BY department) - salary AS gap_from_max FROM employees ORDER BY department, gap_from_max", hint: "Window MAX minus current value.", topic: "Window Aggregates" },
    { id: 129, difficulty: "hard", question: "Find employees who earn more than all employees in the Marketing department.", dataset: "employees", expected_query: "SELECT name, salary FROM employees WHERE salary > (SELECT MAX(salary) FROM employees WHERE department = 'Marketing')", hint: "Subquery with MAX for comparison.", topic: "Subquery" },
    { id: 130, difficulty: "hard", question: "Create a comprehensive sales report with employee name, product name, revenue, and running total per employee.", dataset: "sales", datasets: ["sales", "employees", "products"], expected_query: "SELECT e.name AS employee, p.name AS product, s.revenue, SUM(s.revenue) OVER (PARTITION BY e.name ORDER BY s.sale_date) AS emp_running_total FROM sales s JOIN employees e ON s.employee_id = e.id JOIN products p ON s.product_id = p.id ORDER BY e.name, s.sale_date", hint: "Multiple JOINs with PARTITION BY.", topic: "Window Aggregates" }
  ]
};

if (typeof module !== 'undefined' && module.exports) {
  module.exports = QUESTIONS_DATA;
}
if (typeof window !== 'undefined') {
  window.QUESTIONS_DATA = QUESTIONS_DATA;
}
